{
  "name": "photo-roulette-monorepo",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}